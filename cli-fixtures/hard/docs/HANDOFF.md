Next active task: keep validating Recoil through CLI-only product fixtures, not the LongMemEval bench. The CLI-only hard fixture should exercise init, add, session-evidence ingest, search --json, and wake --json.

Current project truth: Recoil remains local-first. It should avoid a daemon and hosted service for v0.
